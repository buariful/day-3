import React from 'react';

const Loading = () => {
    return (
        <div>
            <button className="btn loading bg-orange-600 mt-14 border-none">loading</button>
        </div>
    );
};

export default Loading;